const prompt = require("prompt-sync")();

/*const lista = [];

do {
    let num = +prompt('numero: ');
    const exist = lista.find(item => item == num)
    if (exist == undefined){
        lista.push(num)
    } else {
        console.log('Esse numero ja existe')
    }
    
    
    var resp = prompt('deseja continur? [S/N] ').toUpperCase()[0];
} while (resp == 'S');

console.log(lista)*/