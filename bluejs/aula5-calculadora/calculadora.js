const nome = 'Calculadoraaaaa'

const soma = (a, b) => a + b;

const sub = (a, b) => a - b;

const mult = (a, b) => a * b;

const divisao = (a, b) => a / b;

module.exports = {
    nome,
    soma,
    sub,
    mult,
    divisao
};